{
  "private": true,
  "name": "alexandraleykauf.com",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "typecheck": "turbo typecheck",
    "pre-commit": "lint-staged && turbo typecheck",
    "prepare": "husky",
    "migrate:generate": "pnpm --filter @alexandraleykauf/contentful-migrations migrate:generate",
    "migrate:run": "pnpm --filter @alexandraleykauf/contentful-migrations migrate:run",
    "migrate:one-off": "pnpm --filter @alexandraleykauf/contentful-migrations migrate:one-off",
    "migrate:status": "pnpm --filter @alexandraleykauf/contentful-migrations migrate:status",
    "migrate:validate": "pnpm --filter @alexandraleykauf/contentful-migrations migrate:validate",
    "generate:types": "pnpm --filter @alexandraleykauf/contentful-types generate",
    "generate:types:watch": "pnpm --filter @alexandraleykauf/contentful-types generate:watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.6.1",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "turbo": "^2.6.1"
  },
  "dependencies": {
    "@antfu/eslint-config": "^6.2.0"
  },
  "lint-staged": {
    "packages/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix"
    ]
  }
}
